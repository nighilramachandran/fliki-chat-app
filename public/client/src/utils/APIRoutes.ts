const host = "http://localhost:3005";
export const regiserRoutes = `${host}/api/auth/register`;
export const loginRoutes = `${host}/api/auth/login`;
export const groupRoutes = `${host}/api/group`;
export const chatGroupRoutes = `${host}/api/chat-group`;
export const getAllGroupRoutes = `${host}/api/all-group`;
export const joinGroupRoutes = `${host}/api/join-group`;
export const addMessagesRoutes = `${host}/api/messages/addmsg`;
